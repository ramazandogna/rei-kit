import {
  Button,
  Card,
  Checkbox,
  Input,
  Modal,
  Select,
  Switch,
  Table,
  Tabs,
  Tooltip,
} from 'ant-design-vue'
console.log(Button, Card, Checkbox, Input, Modal, Select, Switch, Table, Tabs, Tooltip)
