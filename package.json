{
  "name": "rei-kit",
  "author": "Ramazan Doğan",
  "private": true,
  "version": "0.0.1",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "pnpm -r run dev",
    "build": "pnpm -r run build",
    "lint": "pnpm -r run lint",
    "preview": "pnpm -r run preview"
  },
  "devDependencies": {
    "@types/node": "^22.15.27",
    "eslint": "^9.0.0",
    "typescript": "^5.2.2"
  }
}
