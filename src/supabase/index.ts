/**
 * The optional Supabase entry.
 *
 * Kept behind its own export so an app that never touches Supabase does not
 * download the client or its error types.
 */

export const SUPABASE_ENTRY = 'rei-kit/supabase'
