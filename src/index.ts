/**
 * rei-kit — the layer every app starts from.
 *
 * Deliberately empty at v0.0.0: the scaffold is proven to build before a single
 * component moves into it, so the migration that follows has a known-good
 * baseline to fail against.
 *
 * @see https://github.com/ramazandogna/rei-kit
 */

export const VERSION = '0.0.0'
